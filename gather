#!/usr/bin/env zsh

# zsh files
cp -f -p -v ~/.zshrc ./zshrc
cp -f -p -v ~/.tmux.conf ./tmux.conf
mkdir -p oh-my-zsh/custom
mkdir -p oh-my-zsh/themes
cp -f -p -v ~/.oh-my-zsh/custom/*.zsh oh-my-zsh/custom/
cp -f -p -v ~/.oh-my-zsh/themes/imotov.zsh-theme oh-my-zsh/themes/

# editor files
cp -f -p -v ~/.atom/*.cson ~/.atom/*.coffee ~/.atom/*.less ./atom 2>&1 | grep -v 'not copied'
cp -f -p -v ~/.config/micro/*.json ./microcgf 2>&1 | grep -v 'not copied' 

cp -f -p -v ~/.gitconfig ./gitconfig

# need to increase Java heap size on Linux
if [[ $NAME == "grasshopper" ]]; then
  cp -f -p -v ~/.IntelliJIdea2019.3/config/idea64.vmoptions idea/
else
  cp -f -p -v ~/.config/JetBrains/IdeaIC2020.1/idea64.vmoptions idea/
fi